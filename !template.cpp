//include ROSAHOOD
#include <bits/stdc++.h> //all headers
using namespace std; //allows one to use commands like cout, cin
//without the need for using prefix std::

// Fast input-output using cin/cout
#define fast_io ios::sync_with_stdio(false); cin.tie(NULL);

//macro for a for loop, reduces typing need
#define forn(i,a,n) for(long long i = a; i < n; i++)

// Shortcuts 
typedef long long ll; //for long
typedef vector<int> vi; // for dynamic array

// For multiple test cases
void solve() {
    // Sample input-output format
    // Do something 
    // Output result
}
//note: void may be changed to a data type if it is efficient 
//to return some value instead of printing it
//eg: checker code with discrete values (Y/N)

int main() {
    fast_io;
    //input test cases
    //loop using solve()
    return 0;
}
